<view class='page_row'>
  <view class="search">
 
    <input class='carid' placeholder=" {{title}}" focus="{{focus}}" />
    <input class='carnumber' placeholder="请输入转账金额" focus="{{focus}}" />
 
    <button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}" bindtap="primary"> 确认</button>
 
  </view>
  <view class='tip'>
   
  </view>
</view>
    